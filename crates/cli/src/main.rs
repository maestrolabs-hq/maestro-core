//! The `maestro` binary: a thin client that wakes the supervisor if it is not
//! already listening, then asks it.

fn main() {
    eprintln!("maestro: no commands implemented yet");
    std::process::exit(1);
}
